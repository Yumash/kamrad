
// КАМРАД API base URL. Set KAMRAD_API_URL env var to enable external API features.
// Empty by default — no external calls without explicit configuration.
export const KAMRAD_API_DEFAULT_BASE_URL = ''
